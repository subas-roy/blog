/**
 * 1. Create a folder with project name and open in vscode
 * 2. Initialize a "package.json" file in the terminal
 *    -> npm init -y
 *
 * 3. Install express, mongoose, typescript, cors and dotenv
 *    -> npm install express
 *    -> npm install mongoose --save
 *    -> npm install typescript --save-dev
 *    -> npm i cors
 *    -> npm i dotenv
 *
 * 4. Initialize a "tsconfig.json" file
 *    -> tsc -init
 *    -> zUncomment and change: "rootDir": "./" to "rootDir": "./src" and "outDir": "./" to "outDir": "./dist",
 *
 * 5. Create "src" folder in the root folder and create "app.ts" file in the folder. Paste the "Hello world" example from express
 *
 * 6. Create '"build": "tsc"' in "package.json" file to convert typescript to javascript.
 *    -> npm run build
 *    -> Run js file: node ./dist/app.js
 *
 * 7. Create "server.ts" file in the src folder
 *
 * 8. Create ".env" file in root, initialize PORT=5000 & DATABASE_URL=
 *
 * 9. Create database user and copy and paste the username password and write database name in the database url in '.env' file
 *
 * 10. Install declaration files
 *
 *
 *
 */
